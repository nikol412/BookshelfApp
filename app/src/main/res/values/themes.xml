<resources>
    <!-- Base application theme. -->
    <style name="Theme.BookshelfApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>

        <item name="colorOnPrimary">@color/white</item>
                <item name="colorAccent">@color/white</item>

        <item name="colorControlNormal">@color/white</item>

        <item name="android:textColorHint">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
    </style>

    <style name="toolbar_style">
        <item name="titleTextAppearance">@style/text_toolbar</item>
    </style>

    <style name="text_toolbar">
        <item name="android:fontFamily">@font/roboto_medium</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="text_small_description">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/gray_light</item>
    </style>

    <style name="text_small_description.black">
        <item name="android:textColor">@color/black</item>
    </style>


</resources>